压缩列表：
	底层实现是数组；
	列表键和哈希键的底层实现之一；
	是一种为节约内存开发的顺序型数据结构；
	可以包含多个节点，每个节点可以保存一个字节数组或整数值；

当一个列表键只包含少量列表项，并且每个列表项要么是小整数值或较短的字符串，redis就会使用压缩列表来做列表的底层实现。

添加新节点到压缩列表，或者从压缩列表中删除节点，可能会引发连锁更新操作，最坏时间复杂度是O(n^2)，但出现的概率不高。